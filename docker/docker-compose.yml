version: '3.9'
services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    ports:
      - '8000:8000'
    volumes:
      - repo_work:/tmp/irsanai-nexus-work
      - reports:/app/reports_output
volumes:
  repo_work:
  reports:
